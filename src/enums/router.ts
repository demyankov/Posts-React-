export enum AppRoute {
  Main = "/",
  Information = "/information",
  Registration = "/registration",
  Login = "/login",
  Activate = "/activate",
  PostsPage = "/posts",
  AddPost = "/addPost",
  NotFound = "/notFound",
  ResertPassword = "/resert_password",
  ResertPasswordConfirm = "/password/reset/confirm",
}
